1. update the expenseModel.php to to change the spent_amount value of budget table
2. category names (already added or not) using AJAX in add-budget.php

3. Contact reply can be done with ajax
Use AJAX (JavaScript-only, no page reload)
When the "Reply" button is clicked:
Open mailto: link.
Send AJAX request to PHP to update status.
Then, add a new row or table using JavaScript.